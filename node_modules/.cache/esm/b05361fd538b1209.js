let express,translate,fileURLToPath,path;_538‍.w("express",[["default",["express"],function(v){express=v}]]);_538‍.w("translate",[["default",["translate"],function(v){translate=v}]]);_538‍.w("url",[["fileURLToPath",["fileURLToPath"],function(v){fileURLToPath=v}]]);_538‍.w("path",[["default",["path"],function(v){path=v}]]);




translate.engine = 'deepl';

const app = express();
const port = 3000;

const __filename = _538‍._.meta.url.substring(7);
const __dirname = path.dirname(__filename);

app.use(express.static(path.join(__dirname, 'public')));

app.get('/translate', async (req, res) => {
  const text = req.query.text;
  const target = req.query.target;

  try {
    const translation = await translate(text, { to: target });
    res.json({ translation });
  } catch (error) {
    res.status(500).json({ error: 'Translation error' });
  }
});

app.listen(port, () => {
  console.log(`Express is running on port ${port}`);
});